#include <ctype.h>
#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    char word[] = "COMPUTER";
    int len = strlen(word);
    for (int m=0; word[m] != '\0'; m++)
    {
        word[m] = tolower(word[m]);
    }
    printf("%s\n", word);




    for (int z=0; z<= arrLen2; z++)
        {
            point_sum = point_sum + POINTS[index];
        }
}